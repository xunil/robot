[Unit]
Description=flask
After=syslog.target network.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/robot
ExecStart=/home/pi/venv/bin/flask run --host=0.0.0.0 --port=5000
Restart=on-abort

[Install]
WantedBy=multi-user.target
